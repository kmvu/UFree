{
  "name": "ufree-firebase-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore security rules unit tests for UFree",
  "scripts": {
    "test": "bash ./run-tests.sh",
    "test:watch": "mocha --timeout 20000 --watch rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^13.35.1",
    "mocha": "^11.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
