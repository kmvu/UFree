# fastlane/.gitignore
# Sprint 6.1 Security: Protect secrets and build artifacts
# ⚠️ CRITICAL: Never commit credentials, API keys, or passwords

# Environment Variables & Credentials
.env
.env.local
.env.*.local
fastlane/.env

# Apple API Keys (.p8 files - NEVER commit)
*.p8
fastlane/*.p8
fastlane/Keys/*.p8
AuthKey_*.p8

# match: Certificate repo credentials (never commit)
.fastlane_user
.fastlane_password
match_password

# Build artifacts
builds/
*.ipa
*.xcarchive
DerivedData/

# Test results
test_results/
*.test.log

# Temporary files
*.tmp
.DS_Store
Gemfile.lock
